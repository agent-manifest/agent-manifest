User-agent: *
Allow: /

# Build byproducts, not declared representations. Each published Work serves its
# citation as cite.md; the HTML rendering Jekyll produces alongside it duplicates
# that content and must not compete with the Work's own record.
Disallow: /works/*/cite.html

Sitemap: https://agent-manifest-spec.org/sitemap.xml
